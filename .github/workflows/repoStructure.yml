name: repository structure

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Write a multi-line message
        run: |
          echo This demo file shows a very basic and easy-to-understand workflow.
      - uses: the-coding-turtle/ga-file-list@v0.2
        with:
          file_extension: "html"
          ignore_files_starts_with_delimiter: "."
